#=================================================
#   순수 좌표 계산
#=================================================

'''
    기준 좌표와 배열 파라미터로 그리드 전체 좌표 리스트를 계산
    로봇2의 셀 보관함(6구/8구)·셀 삽입 슬롯 좌표 산출용
    AI팀이 넘기는 정수 인덱스를 절대 좌표로 환산

    base: [x, y, z, rx, ry, rz]
    반환: [[x, y, z, rx, ry, rz], ...] (layer -> row -> col 순서)
'''
def generate_grid(base, grid_x, grid_y, offset_x,
              offset_y, num_layers=1, layer_height=0):
    print('그리드 좌표 계산 완료')
